{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile --prod=false && pnpm --filter @smartsense/db generate && node scripts/bundle-api.mjs",
  "framework": null,
  "outputDirectory": "public",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "includeFiles": "packages/db/prisma/**"
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
